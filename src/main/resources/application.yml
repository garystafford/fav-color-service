spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      database: colors

logging:
  level:
    root: INFO

server:
  port: 8091

---

spring:
  profiles: production
  data:
    mongodb:
      host: 10.0.1.6

logging:
  file: fav-color-service.log
  level:
    root: INFO
